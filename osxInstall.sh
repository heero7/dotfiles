# Define steps
# 1. Create a variable that is the base directory for where nvim lives.
# 2. Copy all of the contents of this repository (EXCEPT, ReadMe, .git, .gitignore, .sh, .ps1 files) to directory above
# 3. Tmux install dependencies 
